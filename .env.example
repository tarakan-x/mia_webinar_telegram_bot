# Copy to .env and fill the values
TELEGRAM_BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN
# Optional: Google Sheets
# Leave disabled in config.json if not used
# GOOGLE_APPLICATION_CREDENTIALS is not used; we read credentials_json_path from config.json
# Fișier pentru configurarea token-ului Telegram Bot
# Copiați acest fișier ca .env și completați token-ul real

TELEGRAM_BOT_TOKEN=your_actual_bot_token_here